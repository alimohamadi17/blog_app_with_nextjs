export interface DataProps {
  name: string;
  email: string;
  password: string;
}

export interface CategoryProps {
  _id: string;
  bgColor: string;
  slug: string;
  title: string;
  image: string;
}
